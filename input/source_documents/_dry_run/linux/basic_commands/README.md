# basic_commands

This folder is the temporary Dry Run source input location for the `_dry_run_linux` app and the `basic_commands` category.

Use this folder later to place a very small set of source documents for pipeline verification only.

Do not treat Dry Run source inputs as production inputs.
