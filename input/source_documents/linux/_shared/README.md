# Linux Shared Sources

This folder stores Linux source materials that span multiple categories.

Current contents:

- `intro(1) - intro(8)` Linux manual page overview files
- `Linux man pages online.html`
- `Resources - Linux Foundation - Education.html`

Rule:

- Put category-spanning references here.
- Keep category-specific source files inside `studyapp/input/source_documents/linux/{category}/`.
- When a source can support multiple categories, prefer `_shared` unless one category is clearly primary.

Current note:

- `GNU Coreutils 9.11.html` remains in `basic_commands/` for the current scripted pipeline and as the primary source for Linux basic command questions.
