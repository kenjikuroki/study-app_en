# linux

This app folder is the production app container for Linux question generation.

It is configured to use category-safe `category_id` values that match:

`studyapp/input/source_documents/linux/{category_id}/`

The display names come from the spreadsheet-style `category_name` values.
